---
import { Image } from "astro:assets";
import logo from "/src/images/logo.webp";

const navigationItems = [
  { page: "Create Receipt", path: "/receipt/create-receipt" },
  { page: "Add Payment", path: "/receipt/add-payment" },
  { page: "Orders", path: "/orders" },
  { page: "Customers", path: "/customers" },
];
---

<header>
  <a href="/">
    <Image class="logo" src={logo} alt="" width={115} height={115} />
  </a>
  <ul>
    {
      navigationItems.map((item) => (
        <li>
          <a href={item.path}>{item.page}</a>
        </li>
      ))
    }
  </ul>
</header>

<style lang="scss">
  header {
    max-width: 768px;
    margin: auto;
    display: flex;
    align-items: start;
    justify-content: space-between;
    padding: 0 40px;
  }

  img {
    cursor: pointer;
  }

  ul {
    list-style-type: none;
    display: flex;
    gap: 8px;

    a {
      text-decoration: none;
      color: inherit;

      &:hover {
        text-decoration-line: underline;
      }
    }
  }
</style>
